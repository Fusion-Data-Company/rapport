.env.local
.env*.local
node_modules/
.next/
.vercel/
*.tsbuildinfo
.vercel

# clerk configuration (can include secrets)
/.clerk/

# brainstorm session files (ephemeral, not for source control)
.superpowers/brainstorm/

# uploaded card images (tracked via DB, not git)
public/uploads/**
!public/uploads/**/.gitkeep
